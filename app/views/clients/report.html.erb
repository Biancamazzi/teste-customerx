<p id="notice"><%= notice %></p>

<div class="header-table">
<h1>Relatório</h1>

<table class="table">
  <thead>
    <tr>
      <th>Nome</th>
      <th>Email</th>
      <th>Telefone</th>
      <th>Contatos</th>
      <th>Contatos Email</th>
      <th>Contatos Telefone</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
  <% @clients.each do |client| %>      
    <tr>
      <td><%= link_to client.name, client %></td>
      <td><%= client.email %></td>
      <td><%= client.phone %></td>
      <td><% client.contacts.each do |contact| %>    
        <%= link_to contact.name, contact %><br>
      <% end %>
      </td>
      <td><% client.contacts.each do |contact| %>    
        <%= link_to contact.email, contact %><br>
      <% end %>
      </td>
      <td><% client.contacts.each do |contact| %>    
        <%= link_to contact.phone, contact %><br>
      <% end %>
      </td>


    </tr>
  <% end %>
  </tbody>
</table>
</div>


<br>

<%= link_to 'Novo Cliente', new_client_path %>
